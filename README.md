# もじのおうち

5歳向けの、ひらがなとイラストを結びつけるタッチゲームです。大きな文字のおうちに、対応する絵をタップまたはドラッグして届けます。

## 起動

```bash
pnpm install
pnpm dev
```

進捗とずかんはブラウザーの `localStorage` に保存されます。3つのおうちデータを切り替えられ、`を` と `ん` は専用の特別カードとして収録しています。

GitHub の `main` ブランチへの更新は、Vercel により公開URLへ自動デプロイされます。
